# Проверка иерархии заголовков на сайте

Этот документ описывает инструменты для проверки корректности иерархии заголовков H1-H6 на всех страницах сайта.

## 🎯 Зачем проверять иерархию заголовков?

Правильная иерархия заголовков критически важна для:

- **SEO**: Поисковые системы используют заголовки для понимания структуры и содержания страницы
- **Доступности**: Скрин-ридеры используют заголовки для навигации по странице
- **UX**: Логическая структура заголовков улучшает восприятие контента

## 📋 Правила корректной иерархии заголовков

### ✅ Правильно:
- На каждой странице должен быть **один H1** заголовок
- Заголовки должны следовать **логической последовательности**: H1 → H2 → H3 → H4 → H5 → H6
- **Не пропускайте уровни**: после H1 должен идти H2, а не H3
- H1 должен описывать **основную тему страницы**

### ❌ Неправильно:
- Несколько H1 заголовков на одной странице
- Пропуск уровней (H1 → H3 → H2)
- Отсутствие H1 заголовка
- Использование заголовков только для стилизации

## 🛠 Доступные инструменты

### 1. Полная проверка всех страниц

```bash
node tools/check-headings-hierarchy.js
```

**Что делает:**
- Анализирует все страницы на всех языках
- Показывает структуру заголовков для каждой страницы
- Выявляет проблемы в иерархии
- Создает JSON отчет (`headings-hierarchy-report.json`)
- Показывает общую статистику

**Пример вывода:**
```
📄 Анализ страниц для языка: RU
  ✅ services
  ❌ index
    ⚠️  Пропущен уровень заголовка: переход с h1 на h3
       Заголовок: Студия дизайна
```

### 2. Генерация HTML отчета

```bash
node tools/generate-headings-report.js
```

**Что делает:**
- Создает красивый HTML отчет (`headings-hierarchy-report.html`)
- Интерактивный интерфейс с возможностью сворачивания/разворачивания
- Группировка по языкам
- Цветовая индикация проблем

**Откройте файл `headings-hierarchy-report.html` в браузере для просмотра.**

### 3. Проверка конкретной страницы

```bash
node tools/check-page-headings.js <page_name> [language]
```

**Примеры:**
```bash
# Проверка главной страницы (русский язык по умолчанию)
node tools/check-page-headings.js index

# Проверка страницы контактов на английском
node tools/check-page-headings.js contacts en

# Проверка страницы ресторанов на английском
node tools/check-page-headings.js restaurants-list en
```

**Что показывает:**
- Статус страницы (✅ или ❌)
- Список найденных проблем
- Структуру заголовков
- Рекомендации по исправлению

## 🔧 Как исправить найденные проблемы

### Проблема: Отсутствует H1 заголовок

**Решение:** Добавьте H1 заголовок в JSON файл страницы:

```json
{
  "sections": [
    {
      "name": "promo",
      "data": {
        "heading": {
          "size": "h1",
          "class": "h1 heading uppercase",
          "title": "Заголовок страницы"
        }
      }
    }
  ]
}
```

### Проблема: Пропущен уровень заголовка

**Неправильно:**
```json
{
  "heading": {
    "size": "h1",
    "title": "Главный заголовок"
  }
},
{
  "heading": {
    "size": "h3",  // ❌ Пропущен H2
    "title": "Подзаголовок"
  }
}
```

**Правильно:**
```json
{
  "heading": {
    "size": "h1",
    "title": "Главный заголовок"
  }
},
{
  "heading": {
    "size": "h2",  // ✅ Правильная последовательность
    "title": "Подзаголовок"
  }
}
```

### Проблема: Множественные H1

**Решение:** Измените дополнительные H1 на H2 или другой подходящий уровень:

```json
{
  "heading": {
    "size": "h2",  // Было h1, стало h2
    "title": "Второй заголовок"
  }
}
```

## 📊 Интерпретация результатов

### Типы проблем:

- **❌ Отсутствует H1**: На странице нет главного заголовка
- **⚠️ Множественные H1**: Найдено более одного H1 заголовка
- **⚠️ Пропущен уровень**: Нарушена последовательность уровней
- **❌ Неверный уровень**: Используется недопустимый уровень заголовка
- **💥 Ошибка парсинга**: Проблема с чтением JSON файла

### Статистика:

- **Всего страниц**: Общее количество проанализированных страниц
- **Страниц с проблемами**: Количество страниц с нарушениями иерархии
- **Страниц без H1**: Количество страниц без главного заголовка
- **Процент без проблем**: Показатель качества структуры заголовков

## 🎯 Рекомендации по SEO

### H1 заголовок:
- Должен быть **уникальным** для каждой страницы
- Содержать **основное ключевое слово**
- Описывать **суть страницы**
- Быть **информативным** и **привлекательным**

### H2-H6 заголовки:
- Использовать для **структурирования контента**
- Включать **дополнительные ключевые слова**
- Следовать **логической иерархии**
- Быть **описательными**

## 🔄 Автоматизация

### Интеграция в CI/CD:

Добавьте проверку в ваш процесс сборки:

```bash
# В package.json
{
  "scripts": {
    "check-headings": "node tools/ops/check-headings-hierarchy.js",
    "build": "npm run check-headings && npm run build-assets"
  }
}
```

### Git hooks:

Добавьте pre-commit hook для проверки измененных страниц:

```bash
#!/bin/sh
# .git/hooks/pre-commit
node tools/check-headings-hierarchy.js
if [ $? -ne 0 ]; then
    echo "❌ Найдены проблемы с иерархией заголовков"
    exit 1
fi
```

## 🏆 Лучшие практики

1. **Регулярно проверяйте** иерархию заголовков при добавлении новых страниц
2. **Используйте H1** для описания основной темы страницы
3. **Структурируйте контент** с помощью H2-H6 заголовков
4. **Не пропускайте уровни** в иерархии
5. **Делайте заголовки информативными** и содержательными

## 📚 Дополнительные ресурсы

- [MDN: Heading elements](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/Heading_Elements)
- [W3C: Headings](https://www.w3.org/WAI/tutorials/page-structure/headings/)
- [Google SEO: Use heading tags](https://developers.google.com/search/docs/advanced/appearance/title-link)

---

*Документация обновлена: $(date)* 